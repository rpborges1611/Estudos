//programa teste operações aritméticas
const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //retorna o resto da divisão muito usado para saber se o num epar ou impar

console.log(soma, subtracao, multiplicacao, divisao, modulo)